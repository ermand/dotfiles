return {
  "rcarriga/nvim-notify",
  opts = {
    background_colour = "#2E3440",
    stages = "static",
    timeout = 1500,
    -- level = vim.log.levels.debug,
    -- level = vim.log.levels.error,
    -- level = vim.log.levels.info,
    -- level = vim.log.levels.tracee,
    level = vim.log.levels.warn,
    -- level = vim.log.levels.off,
  },
}
